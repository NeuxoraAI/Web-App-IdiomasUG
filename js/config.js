// App configuration (former canvas prototype props)

// Screen shown on load: 'login' | 'profile' | 'intro' | 'test'
export const START_SCREEN = 'login';

// Exam duration in minutes (5-90 in the original prototype)
export const TIMER_MINUTES = 35;

// When true, the primary button becomes "Comprobar" and shows per-question feedback
export const INSTANT_FEEDBACK = false;

// When true, copying exam text replaces the clipboard with an anti-AI notice
export const AI_GUARD = true;
